# What to Do When a Task is Completed

1. Run `npm run lint` to check for ESLint errors
2. Run `npm run build` to verify TypeScript compiles and Vite build succeeds
3. If Supabase schema changed, create/update a migration in `supabase/migrations/`
4. No automated tests currently exist in the project
