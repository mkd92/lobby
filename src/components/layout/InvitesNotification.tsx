import React from 'react';

// Invites are now handled inside the Team page (/team).
// This component is kept as a no-op so the PageShell import stays valid.
export const InvitesNotification: React.FC = () => null;
